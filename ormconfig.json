{
    "type": "postgres",
    "prot": 5432,
    "host": "localhost",
    "username": "docker",
    "passsword": "ignita",
    "database": "rentalx",
    "migrations": ["./src/database/migrations/*.ts"],
    "cli": {
        "migrationsDir": "./src/database/migrations"
    }
}